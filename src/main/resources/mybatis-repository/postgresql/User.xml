<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.api.repository.UserRepository">

    <select id="countByLoginId" parameterType="string" resultType="int">
        SELECT  COUNT(*)
        FROM 	USER_TB
        WHERE	LOGIN_ID = #{login_id}
    </select>

    <insert id="save" parameterType="user">
        INSERT INTO USER_TB
        (
            USER_ID,
            LOGIN_ID,
            EMAIL,
            USER_NM,
            USER_PW,
            CELL_PHONE,
            CREATED_AT
        )
        VALUES
        (
            nextval('user_seq'),
            #{login_id},
            #{email},
            #{user_nm},
            #{user_pw},
            #{cell_phone},
            now()
        )
    </insert>

    <update id="updateUserPw" parameterType="user">
        UPDATE USER_TB
        SET UPDATED_AT = now()
          , USER_PW = #{user_pw}
        WHERE LOGIN_ID = #{login_id}
    </update>

</mapper>